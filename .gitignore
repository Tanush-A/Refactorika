.venv/
.refactorika/
__pycache__/
*.pyc
.env
demo_repo/.git/

# Fetched benchmark repos (mixed/GPL licenses, ~53MB) — never committed.
eval/external/
# eval/results/ IS committed: it's our reported numbers (JSON + markdown tables).
