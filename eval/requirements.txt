# Refactorika evaluation dependencies.
# Pinned loosely for the hackathon; tighten before relying on results.

# Parsing / analysis (must match refactorika/core, which uses the 0.23+ API)
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0

# Verification-harness gates
pyright==1.1.389
ruff==0.7.4
pytest==8.3.3

# Code-health metrics (4c). Optional at runtime — degrades to None if absent.
radon==6.0.1

# RefactorBench (Phase 2) agent via the Anthropic API. Optional — only needed for
# --refactorbench-provider anthropic. Phase 0/1 run without it.
anthropic>=0.111.0
